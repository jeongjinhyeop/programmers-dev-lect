
// 함수

// fun 함수이름([변수 이름]: 자료형, [변수 이름]: 자료형..): [반환값의 자료형] {
//  내용...
// }

// fun : 함수를 선언하겠다는 키워드
// 함수이름 : 소문자로 시작하는 카멜 표기법을 쓴다.
// 매개변수 : 코틀린의 매개변수는 val이라서 함수 안에서 값을 바꿀 수 없다.
// 반환 자료형 : 함수가 돌려줄 값의 자료형 콜론 뒤에 쓴다.
// return : 값을 돌려주면서 함수를 종료한다.


// 1. 가장 기본이 되는 덧셈 함수
fun add(a: Int, b: Int): Int {
    val result = a + b
    return result
}

fun add2(a: Int, b: Int): Int {
    return a + b
}

// 2. 반환값이 없는 함수 - Unit
// 값을 돌려주지 않고 출력만 하는 함수. 반환 자료형이 Unit이다.
// Unit은 생략할 수 있다.
fun printSum(a: Int, b: Int): Unit {
    println("sum of $a and $b is ${a + b}")
}

fun printSum2(a: Int, b: Int) {
    println("$a + $b = ${a + b}")
}

// 3. 단일 표현식 함수 - 중괄호와 return 생략하기
// 함수의 내용이 표현식 하나뿐이라면 중괄호 대신 등호(=)로 연결할 수 있다.
fun add3(a: Int, b: Int): Int = a + b
// 게다가 반환 자료형도 컴파일러가 추론하므로 생략할 수 있다.
fun add4(a: Int, b: Int) = a + b

// 주의! 중괄호 { }를 사용하는 함수는 반환 자료형을 생략할 수 없다. 추론해주지 않는다.
/*
fun add5(a: Int, b: Int) {
    return a + b
}
 */

// 4. 매개변수에 기본값 주기
fun addWithDefault(a: Int, b: Int = 10) = a + b

// 5. 세 수를 더하는 함수
// 함수 안에서 다른 함수를 부를 수 있다.
fun addThree(a: Int, b: Int, c: Int) = add( add(a, b), c )

// 6. 이름은 같지만 자료형이 다른 함수 - 오버로딩
fun plus(a: Int, b: Int) = a + b
fun plus(a: Double, b: Double) = a + b
fun plus(a: String, b: String) = a + b

// ------------------------------------------------------------
// 예제 1. 함수 호출하기
// ------------------------------------------------------------
fun d_exam1() {
    // 함수 이름 뒤에 괄호를 쓰고, 그 안에 넘길 값(인자)을 적는다.
    val sum = add(10, 20)
    println("add(10, 20) = $sum")               // 30

    // 돌려받은 값은 그대로 다른 곳에 쓸 수 있다.
    println("add(1, 2) = ${add(1, 2)}")         // 3
    println("add(3, 4) + 100 = ${add(3, 4) + 100}")   // 107

    // 변수를 인자로 넘길 수도 있다.
    val x = 5
    val y = 7
    println("add(x, y) = ${add(x, y)}")         // 12

    // 한 번 만든 함수는 몇 번이든 다시 쓸 수 있다. 이것이 함수를 만드는 이유다.
    println(add(1, 1))                          // 2
    println(add(100, 200))                      // 300
    println(add(-5, 5))                         // 0

    println("add2(10, 20) = ${add2(10, 20)}")   // 30
}

// ------------------------------------------------------------
// 예제 2. 반환값이 없는 함수 호출하기
// ------------------------------------------------------------
fun d_exam2() {
    printSum(3, 4)                              // 3 + 4 = 7
    printSum2(5, 6)                             // 5 + 6 = 11

    // 값을 돌려주지 않으므로 결과를 변수에 담는 것은 의미가 없다.
    val nothing = printSum(1, 1)
    println(nothing)                            // kotlin.Unit
}

// ------------------------------------------------------------
// 예제 3. 단일 표현식 함수 호출하기 - 모양만 다를 뿐 결과는 같다
// ------------------------------------------------------------
fun d_exam3() {
    println(add(2, 3))      // 5   중괄호 + return
    println(add2(2, 3))     // 5   중괄호 + return (한 줄)
    println(add3(2, 3))     // 5   = 로 축약
    println(add4(2, 3))     // 5   = 로 축약 + 반환 자료형 생략
}

// ------------------------------------------------------------
// 예제 4. 기본값이 있는 함수 호출하기
// ------------------------------------------------------------
fun d_exam4() {
    println(addWithDefault(5, 3))   // 8    b 에 3 을 넘김
    println(addWithDefault(5))      // 15   b 를 생략 -> 기본값 10 이 쓰임
}

// ------------------------------------------------------------
// 예제 5. 이름 붙인 인자(Named Argument)
// ------------------------------------------------------------
fun d_exam5() {
    // 인자에 매개변수 이름을 붙여서 넘기면 무엇을 넘기는지 한눈에 보인다.
    println(add(a = 10, b = 20))    // 30

    // 이름을 붙이면 순서를 바꿔도 된다.
    println(add(b = 20, a = 10))    // 30

    // 뒤쪽 매개변수만 골라서 넘길 때 특히 유용하다.
    println(addWithDefault(a = 7))          // 17
    println(addWithDefault(b = 100, a = 7)) // 107
}

// ------------------------------------------------------------
// 예제 6. 함수 안에서 함수 부르기
// ------------------------------------------------------------
fun d_exam6() {
    println("addThree(1, 2, 3) = ${addThree(1, 2, 3)}")     // 6
    println("addThree(10, 20, 30) = ${addThree(10, 20, 30)}")   // 60
}

// ------------------------------------------------------------
// 예제 7. 오버로딩 - 넘기는 자료형에 따라 알맞은 함수가 자동으로 선택된다
// ------------------------------------------------------------
fun d_exam7() {
    println(plus(1, 2))                 // 3        Int 버전
    println(plus(1.5, 2.5))             // 4.0      Double 버전
    println(plus("코틀린", "공부"))       // 코틀린공부  String 버전
}

// ------------------------------------------------------------
// 예제 8. 매개변수는 바꿀 수 없다 (참고)
// ------------------------------------------------------------
fun d_exam8() {
    // 코틀린의 매개변수는 val 이라서 함수 안에서 값을 바꿀 수 없다.
    // fun bad(a: Int) { a = 10 }       // 컴파일 에러! Val cannot be reassigned
    // 값을 바꿔야 한다면 함수 안에서 새 변수를 만들어 쓴다.
    println("매개변수는 val 이라 함수 안에서 값을 바꿀 수 없다.")
}

fun main() {
    d_exam1()     // 함수 호출하기
    d_exam2()     // 반환값이 없는 함수 (Unit)
    d_exam3()     // 단일 표현식 함수
    d_exam4()     // 기본값이 있는 매개변수
    d_exam5()     // 이름 붙인 인자
    d_exam6()     // 함수 안에서 함수 부르기
    d_exam7()     // 오버로딩
    d_exam8()     // 매개변수는 바꿀 수 없다
}