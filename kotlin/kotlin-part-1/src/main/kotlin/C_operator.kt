
// * 산술 연산자 : +  -  *  /  %
//   더하기, 빼기, 곱하기, 나누기, 나머지를 구한다.
//   정수끼리 나누면 소수점 이하가 잘리고 정수만 남는다는 점을 주의해야 한다.

// * 대입 연산자 : =  +=  -=  *=  /=  %=
//   오른쪽 값을 왼쪽 변수에 넣는다. +=, -= 처럼 산술 연산자와 결합한 것을 복합 대입 연산자라고 한다.
//   값을 바꾸는 것이므로 반드시 var 로 선언한 변수에만 쓸 수 있다.

// * 증가 연산자(++), 감소 연산자(--)
//   변수의 값을 1 늘리거나 1 줄인다.
//   변수 앞에 붙이면(전위) 값을 먼저 바꾸고 사용하고, 뒤에 붙이면(후위) 값을 먼저 쓰고 나중에 바꾼다.

// * 비교 연산자 : >  <  >=  <=  ==  !=
//   두 값을 비교해 그 결과를 true 또는 false(Boolean)로 돌려준다.

// * 논리 연산자 : &&(그리고)  ||(또는)  !(부정)
//   Boolean 값을 조합해 다시 하나의 Boolean 을 만든다.
//   &&, || 는 결과가 이미 정해지면 오른쪽을 아예 계산하지 않는다. 이를 '단락 평가'라고 한다.

fun main() {

}