
// * 코틀린
// [특징]
// 1. 간결함 : 같은 기능을 자바 대비 약 20~40% 적은 코드로 작성할 수 있다.
// 2. null 안정성 : 타입 레벨에서 null 허용 여부를 구분해 NPE를 컴파일 시점에 막는다.
// 3. 자바와의 100% 상호운용 : 자바 클래스를 그대로 쓰고, 자바에서 코틀린을 호출할 수도 있다.
// 4. 타입 추론 : 타입을 명시하지 않아도 컴파일러가 추론하지만, 여전히 정적 타입 언어이다.
// 5. 함수형 프로그래밍 지원 : 함수가 일급 객체이며 람다, 고차 함수, 불변성을 자연스럽게 쓸 수 있다.
// 6. 코루틴 : 스레드보다 가벼운 방식으로 비동기/동시성 코드를 순차 코드처럼 작성한다.
// 7. 멀티플랫폼 : JVM뿐만 아니라 안드로이드, IOS, JS, 네이티브로도 컴파일된다.

fun main() {
    println("Hello World!")
}